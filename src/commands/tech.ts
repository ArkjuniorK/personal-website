import { Terminal } from "@xterm/xterm";
import { ICommand } from "./interface";

const tech: ICommand = {
  name: "tech",
  desc: "Technology stack",
  contents: [],
  func: (term: Terminal) => {},
};

export default tech;
